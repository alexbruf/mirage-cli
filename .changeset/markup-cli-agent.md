---
"@mirage-cli/markup-cli": patch
---

`MARKUP_AGENT` names the participant on every MCP call (`?agent=`), which Markup records as the author of comments, replies and status changes. A host that signs in once with a shared account can then attribute each action to the person it acts for. Defaults to `markup-cli`.
