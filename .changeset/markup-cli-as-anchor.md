---
"@mirage-cli/markup-cli": patch
---

`--as <name>` sets the participant and author name for one command, overriding `MARKUP_AGENT`, so a host's agent can say who it is acting for. `comment` takes `--selector`/`--tag`/`--markdown` (as `page` prints them) without `--x`/`--y`; the pin anchors to the element. Coordinates remain required for a fixed-point pin.
