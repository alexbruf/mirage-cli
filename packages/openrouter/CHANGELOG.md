# @mirage-cli/openrouter

## 0.2.0

### Minor Changes

- Expose the OpenRouter image model catalog and image-generation commands
  through the Mirage and Cloudflare Workers wrapper.
- Document `images generate` as a billable command that hosts must gate on
  read-only mounts.
